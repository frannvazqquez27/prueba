extends layout

block content
  .container
    h1='Lista completa'
    .row
        .col-12.col-md-3
    ul
      each personaje in personajes
        li
          a(href="/personaje/" + personaje.id) #{personaje.name}